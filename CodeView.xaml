<UserControl x:Class="IO_Mat_tlumacz.CodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IO_Mat_tlumacz"
             x:Name="Root"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
        <ComboBox x:Name="LanguagePick" Grid.Column="0" HorizontalAlignment="Stretch" Margin="8,8,8,8" ItemsSource="{Binding ElementName=Root, Path=DebugList}"/>
        <Button Grid.Column="1" Content="Code Display" Margin="8,8,8,8"/>
        <Button Grid.Column="2" Content="Graphic Display" Margin="8,8,8,8"/>
        </Grid>

        <TextBlock Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="8,8,8,8" FontSize="16"/>

    </Grid>
</UserControl>
